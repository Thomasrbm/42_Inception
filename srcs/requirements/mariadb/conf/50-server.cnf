[mysqld]
user = mysql
datadir = /var/lib/mysql
socket = /run/mysqld/mysqld.sock
pid-file = /run/mysqld/mysqld.pid

# Écouter sur toutes les interfaces pour les autres conteneurs
bind-address = 0.0.0.0
port = 3306

# Options classiques
skip-host-cache
skip-name-resolve
